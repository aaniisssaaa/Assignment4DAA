# 📝 Инструкция: Компиляция в IntelliJ IDEA

## Шаг 1: Открыть проект

1. Запустите IntelliJ IDEA
2. Выберите: **File → Open** (или нажмите Ctrl+O)
3. Перейдите в папку: `C:\Users\user\Downloads\asik4daa`
4. Выберите файл **pom.xml** или саму папку **asik4daa**
5. Нажмите **OK**

## Шаг 2: Дождитесь индексации

- IntelliJ IDEA автоматически определит Maven проект
- Внизу справа появится уведомление "Maven projects need to be imported"
- Нажмите **Import Changes** или **Enable Auto-Import**
- Подождите пока IDE скачает зависимости (JUnit, Gson)

## Шаг 3: Настройте JDK (если нужно)

1. **File → Project Structure** (Ctrl+Alt+Shift+S)
2. **Project Settings → Project**
3. **Project SDK**: выберите Java 11 или выше
4. Если JDK нет в списке:
   - Нажмите **Add SDK → Download JDK**
   - Выберите **Version: 11** или **17**
   - Нажмите **Download**

## Шаг 4: Компиляция проекта

### Вариант A: Через Maven панель (рекомендуется)
1. Откройте панель **Maven** (справа, или View → Tool Windows → Maven)
2. Раскройте **asik4daa → Lifecycle**
3. Дважды кликните **clean**
4. Дважды кликните **compile**
5. Дважды кликните **test** (запуск тестов)

### Вариант B: Через меню Build
1. **Build → Build Project** (Ctrl+F9)
2. Или: **Build → Rebuild Project** (очистка + сборка)

### Вариант C: Через терминал в IDEA
1. **View → Tool Windows → Terminal**
2. Выполните команды:
```bash
mvn clean compile
mvn test
```

## Шаг 5: Запуск программы

### Способ 1: Через Main.java
1. Откройте файл `src/main/java/Main.java`
2. Нажмите зелёную стрелку ▶ рядом с `public static void main`
3. Или: правый клик → **Run 'Main.main()'**

### Способ 2: Через Maven
1. Панель **Maven** → **Plugins** → **exec** → **exec:java**
2. Или в терминале:
```bash
mvn exec:java -Dexec.mainClass="Main"
```

## Шаг 6: Запуск тестов

### В IntelliJ IDEA:
1. Правый клик на папке `src/test/java` → **Run 'All Tests'**
2. Или откройте любой тестовый файл (например, `TarjanSCCTest.java`)
3. Нажмите зелёную стрелку ▶ рядом с классом

### Через Maven:
```bash
mvn test
```

## 🎉 Готово!

После компиляции вы увидите:
- ✅ В папке `target/classes` - скомпилированные .class файлы
- ✅ Зелёные галочки в панели Maven
- ✅ Сообщение "BUILD SUCCESS" в консоли

## 🐛 Решение проблем

### "Maven not found" в IntelliJ
- **File → Settings → Build, Execution, Deployment → Build Tools → Maven**
- Убедитесь что путь к Maven указан правильно
- Или используйте встроенный Maven (Bundled Maven 3)

### "SDK is not defined"
- **File → Project Structure → Project → Project SDK**
- Выберите или скачайте JDK 11+

### Зависимости не загружаются
- Правый клик на `pom.xml` → **Maven → Reload Project**
- Или: **View → Tool Windows → Maven** → кнопка 🔄 (Reload)

### Красные подчёркивания в коде
- **File → Invalidate Caches → Invalidate and Restart**

## 📊 Ожидаемый результат

После запуска `Main.java` вы увидите:
```
=================================================
  Smart City/Smart Campus Scheduling System
=================================================

Processing: small_cycle.json
-----------------------------------------
STEP 1: Finding Strongly Connected Components
-----------------------------------------
=== Strongly Connected Components ===
Total SCCs found: 3
...
```

Все тесты должны пройти успешно (зелёные галочки)! ✅
